<h1>全生徒成績一覧</h1>
<%= form_tag("/users/show_all/")  do %>
  <%= select_tag 'year', options_for_select(@year), {include_blank: '選択して下さい'} %>
  <%= select_tag 'semester', options_for_select([ '1', '2', '3' ]), { include_blank: '選択して下さい' } %>
  <%= submit_tag '表示' %>
<% end %>
<div align="center">
  <table border="20">
    <thead>
    <tr>
      <th>名前</th>
      <th>年度</th>
      <th>学期</th>
      <th>国語</th>
      <th>数学</th>
      <th>英語</th>
      <th>理科</th>
      <th>社会</th>
    </tr>
    </thead>

    <tbody>
    <% @record.each do |rec| %>
      <% rec["records"]["edges"].each do |r| %>
      <tr>
        <td><%= rec["name"] %></td>
        <td><%= r["node"]["year"] %></td>
        <td><%= r["node"]["semester"] %></td>
        <td><%= r["node"]["jpn"] %></td>
        <td><%= r["node"]["math"] %></td>
        <td><%= r["node"]["eng"] %></td>
        <td><%= r["node"]["sci"] %></td>
        <td><%= r["node"]["soc"] %></td>
      </tr>
      <% end %>
    <% end %>
    </tbody>
  </table>
</div>

<%= link_to '戻る', users_teacher_path %>